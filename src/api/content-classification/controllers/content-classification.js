'use strict';

/**
 * content-classification controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::content-classification.content-classification');
